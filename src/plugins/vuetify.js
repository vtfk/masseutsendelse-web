import Vue from 'vue';
import Vuetify, { VDataTable, VDataTableHeader } from 'vuetify/lib'

Vue.use(Vuetify, {
  components: {
    VDataTable,
    VDataTableHeader
  }
});

export default new Vuetify({
});
